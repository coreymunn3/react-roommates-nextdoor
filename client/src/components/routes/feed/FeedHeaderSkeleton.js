import React, { Fragment } from 'react';
import Skeleton from 'react-loading-skeleton';

const FeedHeaderSkeleton = () => {
  return <Skeleton style={{ fontSize: '1.75rem', lineHeight: 1.2 }} />;
};

export default FeedHeaderSkeleton;
